const initialState = {
    "message": "Welcome! Let's get started!",
};


export default function myself(state = initialState, action) {
    switch (action.type) {
        default:
            return state;
    }
}
